server {
    listen 80;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Add additional routes or settings for your app
    # location /api/ {
    #     proxy_pass http://localhost:3000;
    # }
}
